# 前后端联调通过报告

> 模板版本：v1 | 最后更新：{{date}}
> 本文档为可自动填充的报告模板，`{{...}}` 标记为待替换字段。

---

## 一、基本信息

| 字段 | 值 |
|------|-----|
| 应用版本 | `{{app_version}}` |
| Git 提交 Hash | `{{commit_hash}}` |
| 测试日期 | `{{test_date}}` |
| 测试环境 | Windows 10 Pro / Tauri v2 / Node {{node_version}} |
| Rust 版本 | `{{rust_version}}` |
| 数据库路径 | `{{db_path}}` |
| SQLite 版本 | `{{sqlite_version}}` |
| PRAGMA 配置 | foreign_keys=ON, journal_mode=WAL, busy_timeout=5000ms |
| 前端测试 | `{{frontend_test_count}}` tests, `{{frontend_pass_count}}` passed |
| 后端测试 | `{{backend_test_count}}` tests, `{{backend_pass_count}}` passed |
| 报告生成方式 | 手动 / 自动 CI |

---

## 二、本次联调覆盖范围

| 模块 | 覆盖项 | 状态 |
|------|--------|------|
| Bootstrap | 首次启动 / 重启恢复 / inflight repair | ☐ |
| Provider | CRUD / API Key 安全存储 / 连接测试 | ☐ |
| Conversation | 创建 / 重命名 / 归档 / 取消归档 / 删除 CASCADE | ☐ |
| Branch | 创建分叉 / 重命名 / 归档 / 取消归档 / 设主线 | ☐ |
| Message | User 创建 / Assistant placeholder / Complete / Fail / Build prompt | ☐ |
| Streaming | 双层流式架构 / chunk 积累 / flush / complete 提交 | ☐ |
| 树遍历 | 路径遍历 / sibling 排序 / depth 计算 | ☐ |
| 错误处理 | 结构化错误 / TauriAppError 归一化 / console 诊断日志 | ☐ |

---

## 三、联调矩阵结果

> 依据 `前后端联调矩阵+SmokeTest清单.md` 逐项验证。

| 编号 | 场景 | 结果 | 备注 |
|------|------|------|------|
| SMOKE-01 | 首次启动无 provider | ☐ Pass / ☐ Fail / ☐ Blocked | |
| SMOKE-02 | 保存 provider 并重启恢复 | ☐ Pass / ☐ Fail / ☐ Blocked | |
| SMOKE-03 | 创建会话 | ☐ Pass / ☐ Fail / ☐ Blocked | |
| SMOKE-04 | 正常发送消息 | ☐ Pass / ☐ Fail / ☐ Blocked | |
| SMOKE-05 | sendMode=newBranch | ☐ Pass / ☐ Fail / ☐ Blocked | |
| SMOKE-06 | 从历史 assistant 继续 | ☐ Pass / ☐ Fail / ☐ Blocked | |
| SMOKE-07 | 编辑历史 user 消息 ⚠️ | ☐ Pass / ☐ Fail / ☐ Blocked | P0: fork_point 必须正确 |
| SMOKE-08 | regenerate 候选回答 ⚠️ | ☐ Pass / ☐ Fail / ☐ Blocked | P0: 不创建 branch |
| SMOKE-09 | 基于候选继续 | ☐ Pass / ☐ Fail / ☐ Blocked | |
| SMOKE-10 | Compare + 设主线 ⚠️ | ☐ Pass / ☐ Fail / ☐ Blocked | P0: 只改 mainline_branch_id |
| SMOKE-11 | Archive / Unarchive branch | ☐ Pass / ☐ Fail / ☐ Blocked | |
| SMOKE-12 | 删除会话级联删除 | ☐ Pass / ☐ Fail / ☐ Blocked | |
| SMOKE-13 | 重启恢复中断的 streaming ⚠️ | ☐ Pass / ☐ Fail / ☐ Blocked | P0: 无 STREAMING 僵尸 |
| SMOKE-14 | 结构化错误传播 | ☐ Pass / ☐ Fail / ☐ Blocked | |

---

## 四、高风险场景专项结果

### 4.1 History Edit（SMOKE-07）

| 验证项 | 预期 | 实际 | 通过 |
|--------|------|------|------|
| 原消息内容未被修改 | U2 内容不变 | | ☐ |
| 新消息 parent = 原消息的 parent | parent_message_id = A1_id | | ☐ |
| 新消息 edited_from = 原消息 | edited_from_message_id = U2_id | | ☐ |
| 新 branch fork_point = 原消息的 parent | fork_point_message_id = A1_id | | ☐ |
| fork_source_type = HISTORY_USER_EDIT | | | ☐ |

**SQL 验证：**
```sql
-- {{在此粘贴实际查询结果}}
SELECT id, content_text, parent_message_id, edited_from_message_id
FROM messages WHERE edited_from_message_id IS NOT NULL;

SELECT id, fork_point_message_id, fork_source_message_id, fork_source_type
FROM branches WHERE fork_source_type = 'HISTORY_USER_EDIT';
```

### 4.2 Variant Regenerate / Continue（SMOKE-08 / SMOKE-09）

| 验证项 | 预期 | 实际 | 通过 |
|--------|------|------|------|
| Regenerate 不创建 branch | branches 表无新增 | | ☐ |
| Regenerate 不更新 branch head | head_message_id 不变 | | ☐ |
| Variant 与原回答是 siblings | parent_message_id 相同 | | ☐ |
| Variant sibling_index 递增 | index=1 | | ☐ |
| 基于候选继续时 fork_source_type = VARIANT | | | ☐ |

### 4.3 Compare Readonly（SMOKE-10）

| 验证项 | 预期 | 实际 | 通过 |
|--------|------|------|------|
| Compare 模式下 composer 禁用/隐藏 | | | ☐ |
| 设主线后消息数量不变 | COUNT(*) = 操作前 | | ☐ |
| 设主线后 branch 数量不变 | COUNT(*) = 操作前 | | ☐ |
| 设主线后 branch head 不变 | 所有 head 相同 | | ☐ |
| mainline_branch_id 更新为目标 | = R_id | | ☐ |

### 4.4 Mainline Switch

| 验证项 | 预期 | 实际 | 通过 |
|--------|------|------|------|
| 只改 conversations.mainline_branch_id | 仅此列更新 | | ☐ |
| 不修改消息树结构 | 无 messages 表变更 | | ☐ |
| 不修改 branch head | 无 branches.head 变更 | | ☐ |
| 旧 mainline branch DTO isMainline = false | | | ☐ |
| 新 mainline branch DTO isMainline = true | | | ☐ |

### 4.5 Restart Recovery After Streaming Interruption（SMOKE-13）

| 验证项 | 预期 | 实际 | 通过 |
|--------|------|------|------|
| 重启后无 STREAMING 状态消息 | COUNT(*) = 0 | | ☐ |
| 被中断消息状态为 ABORTED | status = 'ABORTED' | | ☐ |
| error_code = APP_RESTART_INTERRUPTED | | | ☐ |
| partial content 被保留 | content_text 非空 | | ☐ |
| Composer 未被禁用 | isSending = false | | ☐ |

---

## 五、数据库不变量检查结果

> 由 `check_db_invariants` command 自动执行。

| 不变量 | 描述 | 通过 | 违例行数 |
|--------|------|------|----------|
| CROSS_CONV_PARENT_MESSAGE | 无跨会话 parent 引用 | ☐ | |
| ORPHAN_BRANCH_NO_CONVERSATION | 无无主 branch | ☐ | |
| MAINLINE_BRANCH_MUST_EXIST | mainline branch 存在且 ACTIVE | ☐ | |
| USER_MESSAGE_MUST_BE_COMPLETED | User 消息状态为 COMPLETED | ☐ | |
| STREAMING_MESSAGE_TIME_LIMIT | 无超过 10 分钟的 STREAMING 消息 | ☐ | |
| BRANCH_HEAD_IN_CONVERSATION | Branch head 属于同一会话 | ☐ | |
| SIBLING_INDEX_UNIQUE | sibling_index 在同一 parent 下唯一 | ☐ | |
| MESSAGE_DEPTH_CONSISTENT | depth 值与 parent 一致 | ☐ | |

**执行命令：**
```bash
# 通过 Tauri DevTools 执行
invoke('check_db_invariants')
```

**结果摘要：**
```
{{粘贴 check_db_invariants 返回的 JSON}}
```

---

## 六、已知问题列表

| 编号 | 严重程度 | 描述 | 复现步骤 | 状态 |
|------|----------|------|----------|------|
| ISS-001 | ☐ P0 / ☐ P1 / ☐ P2 | | | ☐ Open / ☐ Fixed / ☐ Wontfix |
| ISS-002 | | | | |

---

## 七、发布阻塞问题

> P0 问题必须全部关闭才能发布。

| 问题编号 | 描述 | 阻塞原因 | 预计修复 | 负责人 |
|----------|------|----------|----------|--------|
| | | | | |

**发布判定：** ☐ 可发布 / ☐ 不可发布

---

## 八、建议回归项

> 每次迭代必须通过的最低回归集合。

| 编号 | 场景 | 覆盖领域 | 本次通过 |
|------|------|----------|----------|
| REG-1 | 创建会话 + 正常发送（SMOKE-03/04） | 会话 + 消息 | ☐ |
| REG-2 | 编辑历史 user 消息（SMOKE-07） | Non-destructive fork | ☐ |
| REG-3 | regenerate 候选回答（SMOKE-08） | Variant 不创建 branch | ☐ |
| REG-4 | Compare + 设主线（SMOKE-10） | 只改 mainline_branch_id | ☐ |
| REG-5 | Streaming 中断重启修复（SMOKE-13） | 无 STREAMING 僵尸 | ☐ |

---

## 附录 A：环境信息采集脚本

```bash
# 自动填充模板中的 {{...}} 字段
echo "app_version: $(cat package.json | grep version | head -1)"
echo "commit_hash: $(git rev-parse --short HEAD)"
echo "node_version: $(node --version)"
echo "rust_version: $(rustc --version)"
```

## 附录 B：自动化验收 Checklist

- [ ] 前端 101 tests 全部通过
- [ ] 后端 Rust tests 全部通过
- [ ] `check_db_invariants` 返回 `ok: true`
- [ ] PRAGMA foreign_keys = ON 已确认
- [ ] PRAGMA journal_mode = wal 已确认
- [ ] Streaming inflight repair 已执行
- [ ] 无 P0 级已知问题
- [ ] REG-1 ~ REG-5 全部通过
